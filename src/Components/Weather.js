import React from "react";
import { Header, Flag } from "semantic-ui-react";

const Weather = props => (
  <div>
    <Header as="h2">
      <Flag name="al" />
      Shkoder, AL
    </Header>
  </div>
);

export default Weather;
